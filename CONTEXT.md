# CONTEXT.md — 咖啡廳網站領域語言

此檔案是**詞彙表**，只記錄領域語言的定義，不記錄實作細節或架構決策。

---

## 點餐助理（Ordering Assistant）

右下角浮動按鈕觸發的對話式商品推薦功能。透過 2–3 個引導問題，根據使用者喜好從 API 商品中篩選並推薦 2–3 件商品。

## 對話步驟（Step）

點餐助理中的一個問答單位。每個 Step 包含一句助理說的話、若干個選項（Options）。使用者選擇後推進到下一個 Step 或進入推薦結果。

## 分支流程（Branching Flow）

根據使用者在某個 Step 的選擇，決定下一個 Step 是哪一個的邏輯。例如：選擇「喝的」走咖啡/茶路線；選擇「吃的」走甜點路線。

## 對話紀錄（Messages）

使用者與助理的完整訊息陣列，包含助理訊息（assistant）、使用者選擇（user）、推薦結果（recommendation）三種型別。

## 使用者答案（Answers）

對話過程中累積的鍵值對，記錄使用者在每個 Step 的選擇，供推薦邏輯使用。例如 `{ drink: "咖啡", flavor: "濃郁" }`。

## 推薦結果（Recommendation）

對話完成後，根據 Answers 從 API 商品資料中篩選出的 2–3 件商品，直接以商品卡片形式呈現在對話視窗內。

## 商品分類（Category）

API 商品的 `category` 欄位，目前有三個值：`咖啡`、`茶飲`、`甜點`。是 Recommendation 篩選的主要依據。
